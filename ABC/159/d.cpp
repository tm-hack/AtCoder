#include <math.h>

#include <algorithm>
#include <atcoder/all>
#include <iostream>
#include <vector>

using namespace std;

int N;
vector<int> A;

int main() {
    // 入力
    cin >> N;
    int a;
    for (int i = 0; i < N; i++) {
        cin >> a;
        A.push_back(a);
    }

    // 出力
    cout << "Good Luck!" << endl;
    return 0;
}
