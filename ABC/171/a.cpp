#include <bits/stdc++.h>

#include <atcoder/all>

using namespace std;
using namespace atcoder;

int main() {
    // 入力
    char a;
    cin >> a;

    // 出力
    if (islower(a)) {
        cout << 'a' << endl;
    } else {
        cout << 'A' << endl;
    }

    return 0;
}
